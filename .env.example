# Application Settings
PORT=3000
NODE_ENV=development
GLOBAL_PREFIX=api

# Database Settings (PostgreSQL)
POSTGRES_USER=finsh_user
POSTGRES_PASSWORD=finsh_password
POSTGRES_DB=finsh_db
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}?schema=public"

# JWT Authentication Settings
JWT_SECRET=YOUR_VERY_STRONG_SECRET_KEY_HERE
JWT_EXPIRATION_TIME=3600s # 1 hour

# Security Settings
CSRF_SECRET=ANOTHER_VERY_STRONG_SECRET_KEY_HERE

# Email Service (Dev Alternative: Mailtrap or local SMTP)
# EMAIL_HOST=smtp.mailtrap.io
# EMAIL_PORT=2525
# EMAIL_USER=
# EMAIL_PASSWORD=
# EMAIL_FROM=no-reply@finsh-dt.com

# Arduino Integration Settings
# ARDUINO_WEBHOOK_TOKEN=secure_token_for_arduino_webhook
